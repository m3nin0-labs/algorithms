% Criando figura
figure;

% Fase 1
% Definindo grafos
g = graph();
g = addnode(g, 3);
subplot(2,1,1);
plot(g);
title('Fase 1');

% Fase 2
g = addedge(g, 1, 2);

figure
subplot(2,1,1)       % add first plot in 2 x 1 grid
plot(x,y1)
title('Subplot 1')
